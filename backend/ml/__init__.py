"""ML modules for the competition product template.

This package is designed to run out-of-the-box without heavyweight ML
dependencies. If torch / pytorch-grad-cam are installed and configured,
the code can use them; otherwise it falls back to simple baselines.

Teams are expected to replace the placeholder logic with their own models.
"""
